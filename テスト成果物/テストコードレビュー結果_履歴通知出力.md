# テストコードレビュー結果（履歴 / 通知 / 出力）

## 1. レビュー対象

- `tests/code/lib-dateTime-utils.test.mjs`
- `テスト成果物/テストコード実装結果.md`
- `テスト成果物/未実装テストケース_コードベース.md`
- `テスト成果物/未実装テストケース_コードベース_再分類.md`
- 対象実装: `src/components/tasks/titleHistory.ts`, `src/lib/utils.ts`

## 2. 参照資料

- `テスト成果物/テストケース_コードベース.md`
- `テスト成果物/未実装テストケース_コードベース.md`
- `テスト成果物/未実装テストケース_コードベース_再分類.md`
- `src/components/tasks/titleHistory.ts`
- `src/lib/utils.ts`

## 3. レビュー観点

- `TC-CB-*` / `TDxxx` / `TVxxx` / `TAxxx` / `Rxxx` の追跡性
- 対象コードを直接実行しているか
- localStorage / Notification / setTimeout / DOM API モックの独立性
- 未実装ケースの分離妥当性
- 実行コマンドの再現性

## 4. レビュー・修正サマリー

| 優先度 | 観点 | 問題 | 対応 |
|---|---|---|---|
| P2 | 実行安定性 | CSVのBOMを `Blob.text()` の先頭文字として直接期待していたが、実行環境で観測できず失敗した | BOM直接アサートを外し、CSV本文・ダウンロード名・DOM/URL呼び出し検証へ修正 |
| P2 | 未実装管理 | `TC-CB-304` は現行 `exportToCSV` が数式先頭文字防御を実装していないため、通るテストとして実装すると仕様未充足を隠す | `未実装テストケース_コードベース.md` に現行実装未対応として残し、要製品判断を明記 |

## 5. 最終レビュー結果

- P0/P1/P2 の修正対象は残っていない。
- `TC-CB-152〜TC-CB-164`, `TC-CB-166〜TC-CB-172`, `TC-CB-174〜TC-CB-181`, `TC-CB-305〜TC-CB-309` は実装済みとして妥当。
- `TC-CB-304` は未実装として妥当。理由はテスト基盤不足ではなく、製品実装または仕様判断が必要なため。

## 6. 実行結果

```text
node --test tests/code/*.test.mjs
tests 56
pass 56
fail 0
```

```text
npm run test:react
Test Files 4 passed
Tests 25 passed
fail 0
```

```text
node node_modules/typescript/bin/tsc --noEmit -p tsconfig.test.json
exit 0
```

## 7. 残課題

- `TC-CB-304`: CSVセルが `=`, `+`, `-`, `@` で始まる場合にアポストロフィを付与するか、クォートのみを仕様として許容するかの判断が必要。
