# PersonalTaskManage_Re01 開発引き継ぎ資料

作成日: 2026-06-27  
対象リポジトリ: `PersonalTaskManage_Re01`  
ローカルパス: `C:\Users\ryo31\OneDrive\ドキュメント\PersonalTaskManage_テスト管理\PersonalTaskManage_Re01`  
GitHub: `https://github.com/ryo3191007-bit/PersonalTaskManage_Re01.git`  
現在ブランチ: `codex-fix-analytics-session-aggregation`

## 1. 現在の状態

### 1.1 Git状態

現時点で未コミットのファイルがあります。

```text
?? テスト成果物/テスト設計.md
?? テスト成果物/テスト設計_質問票.md
?? テスト成果物/テスト設計レビュー結果.md
?? テスト成果物/引き継ぎ資料_create-test-design_review-test-design.md
?? 開発引き継ぎ資料.md
```

上記は `create-test-design` / `review-test-design` の成果物と、この引き継ぎ資料です。まだ commit / push / PR 作成はしていません。

### 1.2 直近コミット

直近の履歴は以下です。

```text
904fe73 分析集計とテスト成果物を更新
ae09b9a 受入基準と必須成功シナリオを明確化
a64f0c5 Merge pull request #6 from ryo3191007-bit/codex-jst-timezone-rules
c79a50a 日時処理をJST基準に統一
2b2bed2 Merge pull request #5 from ryo3191007-bit/codex-analytics-aggregation-rules
d3c87cf 分析集計ルールを統一
01ee788 Merge pull request #4 from ryo3191007-bit/codex-mobile-ui-and-maintenance
3a7249d Refine scheduled task notifications
```

## 2. アプリ概要

PersonalTaskManage_Re01 は、業務の予定・実績を管理する個人向けタスク管理Webアプリです。

主な機能:

- メール/パスワード認証
- タスク登録・編集・削除
- ステータス変更
- 中断/再開を含む作業セッション記録
- カレンダー表示
- 定常タスク生成
- カテゴリ管理
- 分析画面での予定/実績/差異集計
- ブラウザ通知
- アカウント情報更新
- タスク/カテゴリ一括削除

## 3. 技術構成

```text
React 18
TypeScript
Vite
Tailwind CSS
Supabase Auth / PostgreSQL / Edge Functions
@supabase/supabase-js v2
```

Node / npm条件:

```text
Node.js: ^20.19.0 または >=22.12.0
npm: >=10
```

主要コマンド:

```powershell
npm install
npm run dev
npm run build
npm run typecheck
npm run lint
```

## 4. 重要ファイル

### 4.1 実装

```text
src/App.tsx
src/contexts/AuthContext.tsx
src/contexts/TaskContext.tsx
src/contexts/ThemeContext.tsx
src/lib/dateTime.ts
src/lib/supabase.ts
src/lib/types.ts
src/lib/utils.ts
src/lib/useWorkHours.ts
src/pages/TaskListPage.tsx
src/pages/CalendarPage.tsx
src/pages/AnalyticsPage.tsx
src/pages/RecurrenceGroupsPage.tsx
src/pages/SettingsPage.tsx
src/components/tasks/TaskForm.tsx
src/components/tasks/RecurrenceForm.tsx
supabase/functions/update-account/index.ts
supabase/migrations/
```

### 4.2 ドキュメント

```text
README.md
CLAUDE.md
FUNCTION_LIST.md
SCREEN_LIST.md
COMMON_FUNCTION_LIST.md
```

### 4.3 QA成果物

```text
テスト成果物/テスト計画書.md
テスト成果物/テスト計画レビュー結果.md
テスト成果物/テスト分析.md
テスト成果物/テスト分析_質問票.md
テスト成果物/テスト分析_質問票_回答.md
テスト成果物/テスト分析レビュー結果.md
テスト成果物/テスト設計.md
テスト成果物/テスト設計_質問票.md
テスト成果物/テスト設計レビュー結果.md
```

## 5. アーキテクチャ上の要点

- `App.tsx` はルーターではなくローカル状態で画面を切り替える。
- Provider構成は概ね `ThemeProvider` → `AuthProvider` → `TaskProvider`。
- `AuthContext.tsx` はSupabase認証状態、ログイン/登録/ログアウト、アカウント更新を管理する。
- `TaskContext.tsx` はタスク、カテゴリ、定常グループ、作業セッションの中心的な状態管理・永続化層。
- `CalendarPage.tsx`、`TaskListPage.tsx`、`AnalyticsPage.tsx` はタスク時間計算ロジックの影響を強く受ける。
- 予定/実績/日別/月別の時間計算は `src/lib/dateTime.ts` と `src/lib/utils.ts` を最初に確認する。
- アカウント情報更新は `supabase/functions/update-account/index.ts` 経由。`SUPABASE_SERVICE_ROLE_KEY` はフロントエンド `.env` ではなく Supabase Functions Secrets に置く。

## 6. Supabase関連

### 6.1 フロントエンド `.env`

`.env` には以下のみを置く。

```text
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

注意:

- anon key は公開前提のキーだが、引き継ぎ資料やPR本文には実値を書かない。
- service role key は絶対にフロントエンド `.env` に置かない。

### 6.2 Edge Function Secrets

アカウント情報更新機能には以下が必要。

```text
SUPABASE_SERVICE_ROLE_KEY
```

設定先:

```text
Supabase Dashboard > Edge Functions / Secrets
```

または Supabase CLI:

```powershell
supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
supabase functions deploy update-account
```

### 6.3 マイグレーション

`supabase/migrations/` には12本あります。

最新:

```text
20260621093000_add_recurrence_ends_next_day.sql
```

このマイグレーションは、定常タスクの日跨ぎ対応に必要な `ends_next_day` 系の変更です。新しいSupabase環境では、番号順に全マイグレーションを適用してください。

## 7. 最近決定・実装された重要仕様

### 7.1 通知仕様

- 開始通知: 予定開始時刻に、未着手タスクのみ通知。
- 終了通知: 予定終了時刻に、未完了タスクのみ通知。
- 完了済み・削除済みタスクは通知しない。
- 予定時刻変更時は古い通知を取消して再予約。
- 同じ通知を重複表示しない。
- アプリ起動時点ですでに過ぎた通知は表示しない。
- 予定開始・終了が未入力なら通知は作成しない。
- 通知本文にはタスク名と予定時刻を含める。
- 終了通知本文:
  - 未着手: `終了予定時刻です。まだ開始されていません`
  - 進行中: `終了予定時刻になりました`
  - 中断中: `終了予定時刻です。現在中断中です`

制約:

- 通知は `setTimeout` ベースのため、ページを閉じると通知されない。

### 7.2 分析集計仕様

実績時間:

- 予定時間を実績へ代用しない。
- 未着手: 実績0分。
- 進行中: 終了済みセッション + 現在進行中セッションを現在時刻まで。
- 中断中: 終了済みセッションのみ。
- 完了: セッション合計を優先。
- セッションなし: `actual_time`、次に `actual_start` ～ `actual_end`。
- 実績情報なし: 実績集計から除外。

予定時間:

- 正式な予定工数は `quantity × time_per_unit`。
- 予定開始/終了日時は、日別・月別按分に使う。
- 予定工数が0または未入力なら予定工数未入力扱い。
- 予定日時の長さを予定工数の代用品にしない。

集計対象:

- 子タスクを持つ親タスクは除外。
- 単独タスクと末端の子タスクを集計。
- `track_actual=false` は実績時間・所要時間差異分析から除外。
- ステータス件数・完了率には `track_actual=false` も含める。

丸め:

- 内部計算はミリ秒または小数分のまま。
- 表示時だけ時間を小数第1位へ四捨五入。
- 割合は表示時だけ整数％へ四捨五入。
- テスト期待値は丸め前の分で判定。

所要時間差異:

- `実績 > 予定 + 1分`: 超過
- `実績 < 予定 - 1分`: 短縮
- `±1分以内`: 一致

### 7.3 日時・タイムゾーン仕様

- 業務上の基準タイムゾーンは常に `Asia/Tokyo`。
- 端末のタイムゾーン設定に関係なくJSTで入力・表示・集計。
- DB保存はUTC ISO日時。
- 入力値はJSTとして解釈してUTCへ変換。
- 表示時は明示的に `Asia/Tokyo` を指定。
- 日単位は `[当日00:00 JST, 翌日00:00 JST)`。
- 月単位は `[月初00:00 JST, 翌月月初00:00 JST)`。
- 開始時刻は含み、終了時刻は含めない。
- 日跨ぎ・月跨ぎは重複分だけ按分。
- 終了が00:00の場合、その終了日の占有時間として表示しない。
- 定常タスクの曜日・期間開始・期間終了はJST基準。
- 定常タスクの期間開始日と終了日は両方含む。
- 日を跨ぐ定常タスクは「翌日終了」を明示する。

### 7.4 一括削除仕様

- タスク一覧画面と設定画面に一括削除機能あり。
- 一括削除ボタンは赤色に変更済み。
- 部分失敗時は成功分を確定し、失敗対象を表示する。

### 7.5 モバイルUI

Galaxy S20 5Gを主な想定端末として、360×800相当の表示を重視。

QA成果物では、360×800エミュレーションを正式確認条件として扱っています。

## 8. QA成果物の現在地

### 8.1 テスト計画・分析

以下は作成・レビュー済みです。

- `テスト計画書.md`
- `テスト計画レビュー結果.md`
- `テスト分析.md`
- `テスト分析_質問票.md`
- `テスト分析_質問票_回答.md`
- `テスト分析レビュー結果.md`

分析質問票 `Q001` ～ `Q008` は回答済みです。

### 8.2 テスト設計

`create-test-design` / `review-test-design` は完了済みです。

最終検証結果:

```text
AnalysisTV        : 98
AnalysisTA        : 19
DesignRows        : 98
UniqueDesignTD    : 98
YieldRows         : 98
YieldSum          : 577
BadDesignRows     : 0
BadYieldRows      : 0
MissingTV         : 0
MissingTA         : 0
MissingYield      : 0
ReviewHasNoP0P1P2 : True
QuestionNoOpen    : True
```

ポイント:

- `TV001` ～ `TV098` は `TD001` ～ `TD098` に全件対応。
- Expected Case Yield は全TDに対応。
- 期待TC総数は `577件`。
- 設計質問票は追加質問なし。
- レビュー結果では P0/P1/P2 残課題なし。

### 8.3 次のQA工程

次に実行するなら以下です。

```text
create-test-cases
review-test-cases
```

その後、必要に応じて:

```text
create-test-code
review-test-code
create-playwright-e2e-tests
review-playwright-e2e-tests
execute-codebase-tests
execute-playwright-e2e-tests
create-test-report
review-test-report
```

## 9. 開発時の注意点

### 9.1 開発サーバー起動

このCodex環境では通常のPowerShell PATHに `node` / `npm` がない場合があります。その場合は、CodexバンドルのNode.jsでViteを直接起動してください。

確認済みの起動コマンド:

```powershell
$env:CI = "true"
$wd = "C:\Users\ryo31\OneDrive\ドキュメント\PersonalTaskManage_テスト管理\PersonalTaskManage_Re01"
$node = "C:\Users\ryo31\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
Start-Process -FilePath $node `
  -ArgumentList @("node_modules\vite\bin\vite.js", "--host", "127.0.0.1", "--port", "5173", "--clearScreen", "false") `
  -WorkingDirectory $wd `
  -WindowStyle Hidden
```

起動確認:

```powershell
Invoke-WebRequest -Uri "http://127.0.0.1:5173/" -UseBasicParsing -TimeoutSec 5
```

`HTTP 200` が返れば、in-app browser の `http://127.0.0.1:5173/` を再読み込みしてください。Vite 8 は `CI` が文字列 `"true"` でない場合、非対話プロセスの stdin 終了を検知してすぐ終了することがあります。

### 9.2 OneDrive配下の `node_modules`

過去にOneDriveの削除確認ポップアップ後、Tailwindの `preflight.css` が見つからないViteエラーが発生しました。

症状例:

```text
[plugin:vite:css] [postcss] ENOENT: no such file or directory
... tailwindcss/lib/css/preflight.css
```

対応:

```powershell
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json
npm install
npm run dev
```

ただし、削除系コマンドはユーザー確認の上で実行してください。

### 9.3 `.env`

`.env` はGit管理対象外です。次スレッドで起動確認する場合、`.env` の存在と以下を確認してください。

```text
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
```

キーの実値はチャットやPR本文へ書かないでください。

### 9.4 Supabase Free/Pro

SupabaseのLeaked Password ProtectionはPro以上が必要です。Freeプランでは警告が残る可能性があります。現時点では公開自体のブロッカーではなく、解消するならPro化または別途パスワードポリシー/注意喚起で補完します。

### 9.5 Bolt

修正作業はCodex中心で進めています。Boltは主に公開作業で使っていました。Bolt側DBをSupabaseへClaim済みの経緯があります。DB接続先やAPIキーが変わった場合は `.env` とSupabaseプロジェクトの対応を確認してください。

## 10. 次スレッドで最初にやること

1. 作業ディレクトリへ移動する。

```powershell
cd "C:\Users\ryo31\OneDrive\ドキュメント\PersonalTaskManage_テスト管理\PersonalTaskManage_Re01"
```

2. Git状態を確認する。

```powershell
git branch --show-current
git status --short
```

3. 未コミットのQA成果物を確認する。

```powershell
Get-ChildItem -LiteralPath "テスト成果物" | Select-Object Name,Length,LastWriteTime
```

4. 必要なら検証コマンドを実行する。

```powershell
npm run typecheck
npm run lint
npm run build
```

5. 手元で画面を見ながら修正する場合は、開発サーバーを起動する。

```powershell
$env:CI = "true"
$wd = "C:\Users\ryo31\OneDrive\ドキュメント\PersonalTaskManage_テスト管理\PersonalTaskManage_Re01"
$node = "C:\Users\ryo31\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe"
Start-Process -FilePath $node `
  -ArgumentList @("node_modules\vite\bin\vite.js", "--host", "127.0.0.1", "--port", "5173", "--clearScreen", "false") `
  -WorkingDirectory $wd `
  -WindowStyle Hidden
```

確認:

```powershell
Invoke-WebRequest -Uri "http://127.0.0.1:5173/" -UseBasicParsing -TimeoutSec 5
```

6. QA工程を続ける場合は `create-test-cases,review-test-cases` へ進む。

7. この時点の成果物をGitHubへ反映する場合は、ユーザーに確認してから commit / push / PR 作成する。

## 11. commit / push する場合の候補

今回の未コミット成果物だけを反映するなら、想定コミットメッセージ:

```text
テスト設計とレビュー結果を追加
```

対象:

```text
テスト成果物/テスト設計.md
テスト成果物/テスト設計_質問票.md
テスト成果物/テスト設計レビュー結果.md
テスト成果物/引き継ぎ資料_create-test-design_review-test-design.md
開発引き継ぎ資料.md
```

PR本文では以下を要約するとよいです。

- `create-test-design` / `review-test-design` を実行
- `TV001` ～ `TV098` を `TD001` ～ `TD098` へ展開
- Expected Case Yield 577件を定義
- 設計質問票は追加質問なし
- レビュー結果でP0/P1/P2残課題なし
